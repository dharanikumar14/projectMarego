{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Price list</h1>

    <table border='1' cellpadding='10'>
        <thead>
            <tr>
                <th>Ticket Id</th>
                <th>Category</th>
                <th>priceperticket</th>
                
                
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('price_show', { 'id': entity.ticketid }) }}">{{ entity.ticketid }}</a></td>
                <td>{{ entity.categoryid }}</td>
                <td>{{ entity.pricesperticket }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('price_edit', { 'id': entity.ticketid }) }}">Edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('price_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
