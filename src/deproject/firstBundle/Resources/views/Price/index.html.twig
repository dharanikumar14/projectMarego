{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Price list</h1>

    <table border='1' cellpadding='10'>
        <thead>
            <tr>
                <th>Ticket Id</th>
                <th>Category</th>
                <th>priceperticket</th>
                <th>Actions</th>
                
                
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.Ticketid.tName }}</td>
                <td>{{ entity.Categoryid.cName }}</td>
                <td>{{ entity.Priceperticket }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('price_edit', { 'id': entity.Ticketid.tName }) }}">Edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('price_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
