{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PriceCategory list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cid</th>
                <th>Cname</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pricecategory_show', { 'id': entity.cId }) }}">{{ entity.cId }}</a></td>
                <td>{{ entity.cId }}</td>
                <td>{{ entity.cName }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pricecategory_show', { 'id': entity.cId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pricecategory_edit', { 'id': entity.cId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pricecategory_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
