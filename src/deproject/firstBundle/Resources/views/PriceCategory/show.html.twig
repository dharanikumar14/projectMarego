{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PriceCategory</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Cid</th>
                <td>{{ entity.cId }}</td>
            </tr>
            <tr>
                <th>Cname</th>
                <td>{{ entity.cName }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pricecategory') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pricecategory_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
