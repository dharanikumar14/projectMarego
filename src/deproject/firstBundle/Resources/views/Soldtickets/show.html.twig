{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Soldtickets</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Tid</th>
                <td>{{ entity.tId }}</td>
            <tr>
                <th>Cid</th>
                <td>{{ entity.cId }}</td>
            </tr>
            <tr>
                <th>GId</th>
                <td>{{ entity.gId }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ entity.quantity }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ entity.pricePerTicket }}</td>
            </tr>
            <tr>
                <th>date Created</th>
                <td>{{ entity.date_created }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('soldtickets') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('soldtickets_edit', { 'id': entity.tId }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
