{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GrantType creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('granttype') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
